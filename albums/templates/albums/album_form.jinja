{% extends 'base.jinja' %}

{% block content %}
<h2>{{page_title}}</h2>

<form method='post'>
    
    {% csrf_token %}
    {{ form|bootstrap }}

    <div class="form-group">
        <input type="submit" value="Save" class="btn btn-primary" />
    </div>

</form>

{% endblock %}